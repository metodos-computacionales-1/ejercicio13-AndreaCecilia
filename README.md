# ejercicio13-AndreaCecilia


Exercise 2
¿Que pasa si se le pone cons? 
Cuando se corre el codigo con la referencia el valor de la variable x cambia, en cambio cuando es por valor esto no ocurre. En el momento que se define como contaste la referencia del primer parametro de entrada en la funcion, lo que ocurre es que se está definiendo que este valor no puede cambiarse, por lo que luego el codigo no compila, ya que dentro de la funcion se intenta cambiar el valor de a por el de r, lo que genera un error, ya que se le esta dando a la maquina instruccuiones contradictorias. 


Exercise 4
What would be the difference?
Que hora se tiene una funcion pero sobrescrita, en la cual se ejecuta de manera distinta segun el tipo de parametros que le entren como entrada.


//Exercise
//Compile the following code, is it correct?
Se generea un error al compilar, con la bandera se especifica a que se debe de forma mas explicita.
Al usr Heap se permite rocesar mas datos


